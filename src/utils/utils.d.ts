// Esse arquivo é para ajudar o auto-complete do VSCode a identificar os types.

export function applyTemplates(
  data: object,
  string: string,
  delimiters?: string[]
): string;
export function wait(ms: number): Promise<void>;
